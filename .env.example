# ZeroLink Environment Variables

# This file contains example environment variable configurations for ZeroLink.
# Copy this file to .env.local and fill in your actual values.

# ================================
# GEMINI API KEYS (REQUIRED)
# ================================
# ZeroLink uses Google's Gemini API for natural language to logic conversion.
# You need at least one API key. Multiple keys enable rotation and rate limit handling.
# Get your keys from: https://makersuite.google.com/app/apikey

GEMINI_API_KEY_1=your_primary_gemini_api_key_here
GEMINI_API_KEY_2=your_secondary_gemini_api_key_here  # Optional but recommended
GEMINI_API_KEY_3=your_tertiary_gemini_api_key_here   # Optional for high traffic

# Note: Keys are automatically discovered by the key rotator.
# Add as many GEMINI_API_KEY_* variables as needed.

# ================================
# FIREBASE (OPTIONAL)
# ================================
# Firebase is used for cloud sync of saved logic rules.
# If not configured, the app will use localStorage only (fully offline).
# Get your config from: https://console.firebase.google.com/

NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789012
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789012:web:abcdef123456

# Note: These are NEXT_PUBLIC_* variables and will be exposed to the client.
# This is safe - Firebase API keys are not secret (protected by Firestore rules).

# ================================
# NEXT.JS (OPTIONAL)
# ================================
# These variables control Next.js behavior

# NODE_ENV=production  # Automatically set by build commands
# NEXT_PUBLIC_VERCEL_URL=your-app.vercel.app  # Auto-set by Vercel

# ================================
# ANALYTICS (OPTIONAL)
# ================================
# Vercel Analytics and Speed Insights are automatically enabled when deployed to Vercel.
# No additional configuration needed.

# ================================
# DEVELOPMENT ONLY
# ================================
# Uncomment these for local development

# NEXT_TELEMETRY_DISABLED=1  # Disable Next.js telemetry
# DISABLE_ESLINT_PLUGIN=false  # Enable ESLint in development
